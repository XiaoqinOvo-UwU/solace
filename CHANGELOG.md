# 更新日志

> 主页「更新公告」卡从这里读取最新一版的更新摘要。每条尽量简短，避免超出卡面。

## v5.1.0 — 共同经历 · 查看记忆 · 召回 · 修复
- **修复（重要）**：AI 回复不落库 / 卡"正在输入" / 后续消息发不出去（`replyTimer` 调错对象，v4.5.5 引入）
- 修复：AI 偶发把内部状态 JSON 念出来（出站过滤补强：裸 JSON 行也拦）
- 修复：拉取模型列表时的悬垂定时器隐患；LLM 请求改用 Qt 传输超时
- 「共同经历」重写：只在真正有意义的时刻记录（首次 / 久别重逢 / 深度倾诉 / 关系升级），带去重与质量门槛
- 「查看记忆」显示 v5.0 个人模型（每条记忆 + 为什么重要）
- 记忆一致性：清空记忆 / 手动编辑不再丢个人模型；个人模型去重
- 召回打分纳入 category/emotion（情绪匹配权重更高）

## v5.0.0 — Memory Core（记忆变成"个人模型"）
- 记忆不再只是事件记录：每条带 category / emotion / relationship / importance / confidence，知道"为什么重要"
- 新记忆与事件自动标注（结构化 LLM + 启发式兜底）
- 提示词注入「我为什么记得这些」，并按当前情绪加权（难过时优先想起难过的那几次）
- 旧记忆自动迁移到 memoryCore[]，不丢数据

## v4.6.4 — 聊天状态修复
- 切联系人 / 重载会话时清掉残留的"正在输入 / 发送中"状态，头部不再卡住、消息不再被吞
- AI 上下文里的用户历史改用 `用户:` 前缀，修复用户历史被角色解析丢弃的问题
- 切联系人后，属于旧会话的 AI 回复只写回旧会话，不再串到当前会话

## v4.6.3 — 更新更稳更快
- 更新检测：直连 GitHub API 失败时自动回退到代理镜像，外国节点不再检测失败
- 下载：官方直链 + 镜像一起测速，从最快源下载，失败自动逐个重试
- 校验值获取同样支持镜像回退

## v4.6.2 — 修复
- 修复上传头像后占位文字仍遮在头像上的问题
- 修复 AI 偶发把内部规则念给用户的问题

## v4.6.1 — 文档完善
- README 增加项目 Wiki 链接

## v4.6.0 — 更名为 Solace
- 项目更名为 Solace（仓库 / 界面 / 安装程序）
- 更新源迁移到新仓库，旧版本仍可自动升级到新版
- 保留原有配置与记忆（数据目录不变）

## v4.5.5 — 聊天自动贴底修复
- 修复 AI 来消息后不自动滚到底部的问题
- 停在底部自动跟随（含换行消息），上滑翻历史仍不回弹

## v4.5.4 — 更新体验修复
- 修复版本号残留（本地已最新却提示旧版本）
- 移除每小时的更新灵动岛（结果只在卡面与设置面板）
- 卡面点「更新」直接下载并显示进度圆圈；设置里更新则卡面不显示

## v4.5.3 — 更新检测更省心
- 每小时静默检测更新，结果只显示在首页「更新公告」卡
- 全屏游戏时自动跳过检测，不抢性能
- 点卡片直接跳到「设置 → 维护/更新」下载（不再弹灵动岛）

## v4.5.2 — 自动更新检测
- 启动自动检测新版本，发现即弹灵动岛提示，可一键更新
- 首页「更新公告」显示真实版本状态，点卡片即可更新

## v4.5.1 — 聊天修复 · 角色卡
- 修复聊天上滑被强制锁底部（不再因布局抖动回弹）
- AI 连续多条消息折叠头像，与用户侧一致
- 回复不再总是两条；新增结构化角色卡 + 示例对话 + 关键词世界书

## v4.5.0 — 主页 · 活人感 · 主动聊天
- 新增首页：AI 日报 / 更新公告 / GitHub 今日热门
- 记忆修复：去重、清理垃圾事件、未完待续自动记录
- AI 更活人；主动聊天放宽，不再被编程状态封死

## v4.4.2 — 主页与聊天体验
- 聊天新增「已读」回执与「离线」状态
- 修复聊天列表贴底：不漏消息、不回弹
- 主页新增 AI 日报 / 更新公告 / GitHub 热门

## v4.4.1 — 灵动岛打磨
- 连续弹出不再被上一段动画误杀
- Esc 关闭 + 焦点落在首个按钮 + 无障碍播报

## v4.4.0 — 聊天页重做
- 多行输入 + 流式打字气泡 + 回到底部浮钮
- 修复列表滚动回弹：不再强制贴底

## v4.3.1 — 记忆与问候
- AI 自主决定记忆存储（按重要度）
- 修复侧栏问候不随小时刷新
